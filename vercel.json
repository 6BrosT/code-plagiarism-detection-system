{
  "version": 2,
  "name": "code-plagiarism-backend",
  "builds": [{ "src": "src/index.js", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "/src/index.js" }],
  "buildCommand": "npm install -g phantomjs-prebuilt phantomjs && yarn build",
  "installCommand": "yarn install"
}
